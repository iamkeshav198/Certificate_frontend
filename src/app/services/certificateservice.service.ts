import { Injectable } from '@angular/core';
// import { jsPDF } from 'jspdf';
import html2canvas from 'html2canvas';

@Injectable({
  providedIn: 'root'
})
export class CertificateserviceService {

  constructor() { }
  // public generateCertificatePdf(filename: string, htmlContent: string): void {
    // Create a new PDF document
    // const doc = new jsPDF();

    // Add the certificate content to the PDF document
    // doc.html(htmlContent);

    // Save the PDF document as a file
    // doc.save(filename);
  // }
}
